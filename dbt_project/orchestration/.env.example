# Databricks connection settings
DATABRICKS_HOST=your-databricks-host.cloud.databricks.com
DATABRICKS_TOKEN=your-databricks-token

# Email notifications (optional)
AIRFLOW__SMTP__SMTP_HOST=smtp.gmail.com
AIRFLOW__SMTP__SMTP_PORT=587
AIRFLOW__SMTP__SMTP_USER=your-email@gmail.com
AIRFLOW__SMTP__SMTP_PASSWORD=your-app-password
AIRFLOW__SMTP__SMTP_MAIL_FROM=your-email@gmail.com

# dbt specific settings
DBT_DATABRICKS_SEARCH_PATH=ted_dev

# ==========================================
# SETUP INSTRUCTIONS
# ==========================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace the values above with your actual credentials
# 3. DATABRICKS_HOST should be: your-workspace.cloud.databricks.com (without https://)
# 4. DATABRICKS_TOKEN should be your personal access token from Databricks
# 5. Never commit the .env file to version control (it's in .gitignore)
# 
# For Adventure Works Analytics project:
# - Replace 'your-databricks-host' with your actual Databricks workspace URL
# - Replace 'your-databricks-token' with your personal access token
# - Email settings are optional for notifications